---
name: ci-test-plugin
description: "CI pipeline test plugin. Trigger phrases: test plugin, ci test, hello plugin"
license: MIT
metadata:
  author: yz06276
  version: "0.1.0"
---

# CI Test Plugin

A minimal plugin to validate the plugin-store CI pipeline (Phase 1-5).

## Commands

```bash
ci-test-plugin hello [name]    # Say hello
ci-test-plugin health          # Health check
ci-test-plugin --version       # Show version
```
